Load Data  (use import os)

script_dir = os.path.dirname(__file__) #takes file directory
file_path = os.path.join(script_dir, 'sales_data_3.csv')



Check the info with:

df = pd.read_csv(file_path, header=0)
print("\n Data info: ")
df.info()
print('\nData head: ')
print(df.head(3))
print('\nCheck for null data: ')
print(df.isnull().sum())



Clean Data - remove nulls with 

print('\nCheck for null data: ')
print(df.isnull())
df.dropna(axis=1, how='all', inplace=True) #remove all columns if that column has all NaNs
print("\nData after dropping rows with null values:")
print(df.head(3))



check data types one more time just in case
print(df.dtypes)

