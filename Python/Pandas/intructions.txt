Load the Data:

Load the data from sales_data.csv into a Pandas DataFrame.
Display the first 5 rows of the DataFrame.
Data Cleaning:

Check for any missing values and handle them appropriately.
Ensure the data types are correct (e.g., Date should be a datetime object).
Basic Analysis:

Calculate the total sales (Quantity * Price) for each transaction and add it as a new column Total_Sales.
Find the total sales for each store.
Data Aggregation:

Calculate the total quantity sold and total sales for each product.
Calculate the average sales per transaction for each store.
Data Visualization (Optional):

Plot the total sales for each store.
Plot the total quantity sold for each product.

